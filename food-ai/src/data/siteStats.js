// src/data/siteStats.js
// Public-facing counters — bump as the catalog and scan volume grow.

/** Approximate products in the database (shown with a +). */
export const SITE_PRODUCTS_APPROX = 1000;

/** Approximate scans processed (shown with a +). */
export const SITE_SCANS_APPROX = 10000;

export const SITE_MODES_COUNT = 3;

/** Star scale maximum (displayed with ★). */
export const SITE_SCALE_STARS = 5;
